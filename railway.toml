[build]
builder = "DOCKERFILE"
dockerfilePath = "python/Dockerfile"
watchPatterns = ["python/**", "railway.toml"]

[deploy]
healthcheckPath = "/"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
