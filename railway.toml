# v3.7.171 deploy trigger
[build]
builder = "DOCKERFILE"
dockerfilePath = "python/Dockerfile"
watchPatterns = ["python/**", "railway.toml"]

[deploy]
healthcheckPath = "/"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
