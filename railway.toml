# v3.7.163 deploy trigger
[build]
builder = "DOCKERFILE"
dockerfilePath = "python/Dockerfile"
watchPatterns = ["python/**", "railway.toml"]

[deploy]
healthcheckPath = "/"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
