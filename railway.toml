[build]
builder = "DOCKERFILE"
dockerfilePath = "python/Dockerfile"
watchPatterns = ["python/**"]

[deploy]
healthcheckPath = "/keepalive"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
